# Base URL of the backend the UI proxies to. Copy to .env.local to override the
# default (http://localhost:8000). Must be NEXT_PUBLIC_-prefixed to be readable
# at build/run time; it is consumed by next.config.js rewrites.
NEXT_PUBLIC_API_URL=http://localhost:8000
