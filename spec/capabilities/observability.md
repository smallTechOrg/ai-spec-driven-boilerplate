# SUPERSEDED

This capability file described LangSmith-based observability. LangSmith and LangChain are no longer used.

Observability is now a cross-cutting concern in every phase slice: structlog JSON logs to stdout, one log line per operation. See `spec/architecture.md` for the observability stack, and `spec/agent.md` for the per-node log events.
