# Spec doc review fixes (R1 adversarial review) — harmonization reference

## `/Users/sai/Workspace/Code/exp1/spec/vision.md` — score 4/5 (revise)
**Stub-passable sections:**
- Product Definition — typed input/output not forced; a true-but-empty one-sentence statement passes.
- Users & Jobs — (→ SC-N) link is checked but the quantity is not required to match the referenced SC.
- Problem & Current Baseline — unbounded [ASSUMPTION] lets invented numbers from any source satisfy the baseline.
- Success Criteria — COVERAGE (happy/UX/stub/Unwanted) is a comment, not enforced rows; can ship 3 happy-path SCs and pass.
- Success Criteria — Acceptance-test cell accepts a plausible pytest node id that asserts nothing real.
- Hard Constraints — pre-seeded example values mean uncommenting the template substitutes for an actual decision.
**Missing for executor:**
- The concrete column schema / field list of the INPUT and OUTPUT artefacts (vision names format+size but not fields).
- Per-SC mapping to the exact api.md endpoint / data-model entity that satisfies it (trace only goes down to phases).
- Runtime latency budget and per-query timeout as authoritative constraints (only a build-time ceiling is forced).
- Concurrency and data-volume envelope (max concurrent sessions, max result rows/columns).
- The complete product-level failure taxonomy beyond the single required IF…THEN SC.
**Fixes:**
- Product Definition: add a mandatory 'Input contract' and 'Output contract' mini-table (Field | Type | Constraint) using the SAME typed tokens as api.md (string|int|float|bool|ISO-8601|array<T>|object{…}), OR a required link line 'Full input/output shape: spec/api.md §<endpoint>' that the gate checks resolves to a non-placeholder endpoint. Reject the section if neither the typed mini-table nor a resolving api.md link is present.
- Users & Jobs: strengthen the HARD BAR from 'ends with (→ SC-N) that exists' to 'the quantity in the Success-signal cell MUST appear verbatim in the referenced SC-N row'. Add an explicit gate check that the number/threshold matches, so a 5 s signal cannot point at an SC with no 5 s.
- Problem & Current Baseline: bound the [ASSUMPTION] escape — require the 'Source' column to be one of {measured, vendor-doc, prior-build-log, user-stated} and forbid bare 'gut feel'/'estimate'. Add: 'An [ASSUMPTION] with no verifiable source class is treated as [NEEDS CLARIFICATION] and blocks the gate until the user confirms.'
- Success Criteria: convert the COVERAGE comment into FOUR pre-labelled, non-deletable rows the researcher must fill — `SC-CORE`, `SC-UX`, `SC-STUB`, `SC-FAIL` (the IF…THEN one) — so a missing category is a visibly empty required row, not a silent omission. Keep free SC-N rows for the rest. Make the gate assert all four labelled rows are non-empty and use the right EARS form (SC-FAIL must be IF…THEN).
- Success Criteria: require every Acceptance-test cell to name BOTH (a) the artefact location (pytest node / curl / DOM selector) AND (b) the asserted literal in a `assert <lhs> == <value>` or `assert <count> >= <n>` form — i.e. demand a parseable assertion, not prose. Add a weak example of a named-but-empty test ('pytest …::test_query — runs the query') vs strong ('… asserts rows length >= 1 and rows[0]["value"] is not None') so the empty-assertion failure mode is explicitly banned.
- Hard Constraints: add a REQUIRED 'Runtime budget' row (e.g. p95 query latency, max rendered rows, per-query timeout) and a 'Concurrency / volume envelope' row (max concurrent sessions, max result rows/columns) — both currently absent. Mark them REQUIRED like the port rows.
- Hard Constraints: stop pre-seeding answer values inside the example comments in a way that lets uncommenting substitute for deciding. Replace `<!-- :8001 -->` style with `<!-- FILL IN: backend port (harness default :8001 — confirm or override) -->` and have the gate flag any row whose value is byte-identical to the template comment, forcing an explicit confirmation.
- Cross-doc traceability: extend the TRACEABILITY LAW so every SC-N must ALSO cite the api.md endpoint or data-model entity that proves it (SC ↔ contract), not only a delivery-plan phase. This closes the gap where vision is fully filled but the technical contracts that an executor needs are still stubs.
- Repo-wide consistency (blocking): resolve the ROADMAP contradiction. vision.md line 169-170 and delivery-plan.md (lines 10, 49, 277) state 'there is NO separate ROADMAP.md — defer to a phase number in delivery-plan.md', but spec/README.md line 16 and spec/ROADMAP.md present ROADMAP.md as the canonical home for deferred scope. A researcher filling vision's Non-Scope '→ Phase N' dispositions is pointed at a contract the rest of the repo contradicts — a one-fact-one-place violation. Pick one home and update README.md or delete ROADMAP.md (or update vision/delivery-plan) so the deferral target is unambiguous.

## `/Users/sai/Workspace/Code/exp1/spec/architecture.md` — score 4/5 (revise)
**Stub-passable sections:**
- Stack > Rationale column: the instruction says 'tie each Rationale to a constraint or SC-N where possible' — 'where possible' is an escape hatch. A researcher can write 'async + Pydantic v2' (matching the green example) with NO SC-id and pass. Nothing MACHINE-CHECKABLE forces an SC reference, so the rationale can stay generic prose.
- Component Topology > Responsibility column: 'ONE sentence' is the only constraint. 'Runs the graph in agent-graph.md' is one sentence and stub-passable — it names no actual node/module. The bar is sentence-COUNT, not content; a researcher can write one vague sentence per row and satisfy it.
- Component Topology diagram: enforcement is structural (every node↔row both ways, edges labelled with transport+port) which is good — BUT 'transport' tokens are not enumerated. A researcher can label an edge 'API call' or 'data' instead of a real transport (HTTP/in-process/file I/O) and nothing in the template rejects it (contrast api.md which gives an explicit allowed-token list).
- Inter-Component Contracts > On-failure: the prose says '"Handle the error"/"log it" alone is REJECTED' and 'SHOULD correspond to a row in Failure Modes' — but SHOULD is non-binding and there is no rule that every Inter-Component on-failure action MUST appear as a Failure Modes row. A researcher can write a plausible-but-untested action ('return 500') that never gets a detection mechanism or EARS criterion.
- Phase Notes: 'Contract frozen for later swap' column has no required format. A researcher can write '/query response shape' (the example) without pointing to the actual section/anchor in api.md that holds that shape. 'References the frozen stub contract' is asserted in prose but not structurally forced — a bare phrase passes.
- Startup Sequence steps: each step must be 'an OBSERVABLE state transition' but only ONE step (the refuse-to-start) has a concrete EARS test attached. Steps 1, 3, 4, 5 can be filled with the green-example text verbatim and have no acceptance test of their own, so 'observable' is asserted but only enforced for step 2.
**Missing for executor:**
- Failure-mode → EARS linkage is one-directional and unverifiable from this file alone. The Recovery cell cites 'PN-ACn' but the criteria live in delivery-plan.md; nothing here forces that the cited id EXISTS. An executor reading only architecture.md cannot confirm the failure has a real test backing it — the citation can dangle.
- No concurrency / single-writer contract surface. data-model.md has a 'Concurrency note' row that cross-refs 'architecture.md Failure modes (DB locked)', but architecture.md itself never requires a row stating the writer model (single-writer vs pooled). The DB-locked failure mode appears, but the executor must invent whether writes are serialized, what the lock-acquire timeout is, and the retry/backoff numbers (the green example hardcodes '×3 with backoff' but no backoff base/cap is required).
- Retry semantics are under-specified where they appear. 'retry ×3 with backoff' (Inter-Component + Failure Modes) names a count but not the backoff base, cap, or jitter — an executor will invent these. The template should force an explicit retry policy (count, base ms, cap ms, jitter y/n) or 'no retry'.
- Health/readiness vs liveness distinction is absent. Startup step 5 cites /health returning stub_mode, but the template never forces the spec to say whether the process is ready-to-serve BEFORE create_tables() completes (step 3 vs step 4 ordering is given, but there is no required statement that /health must 503 until bootstrap finishes). An executor must invent the not-ready behaviour.
- Logging/observability contract is not anchored here despite APP_LOG_LEVEL living in the env registry. 'surface request id' appears in the green examples but the template never requires defining the request-id format/propagation or what gets logged on each failure — the executor invents the log schema (contrast: the repo has an observability pattern that is never linked from this template).
- Idempotency / restart safety of startup is named only for schema (CREATE TABLE IF NOT EXISTS) but not for the process as a whole — e.g. what happens on a second start with a partially-written DB file. Executor must decide.
**Fixes:**
- Stack > Rationale: delete the 'where possible' escape. Make the column REQUIRED to end with a parenthesized id: '(→ SC-N)' or '(→ constraint: <name>)'. Add to the FILL-IN: 'A rationale with no parenthesized constraint/SC id is REJECTED' — mirror api.md's hard '≥1 required' trace rule.
- Component Topology: enumerate the allowed transport tokens exactly as api.md enumerates type tokens. Add: 'Allowed transport tokens: HTTP/JSON | HTTPS | in-process | file I/O | WebSocket | stdin/stdout. Any other label is REJECTED.' This closes the 'API call'/'data' loophole.
- Component Topology > Responsibility: raise the bar from sentence-count to content. Require the responsibility to name the file/section it owns: 'one sentence that names the artefact it owns (a section of api.md / a node in agent-graph.md / a table in data-model.md)'. A responsibility that names no concrete artefact is rejected.
- Inter-Component Contracts: change 'SHOULD correspond to a row in Failure Modes' to MUST, and add a structural check: 'Every on-failure action whose transport can fail MUST have a matching Failure Modes row; the analyser flags any on-failure action with no detection mechanism in Failure Modes.' Also add a required Retry column or force the On-failure cell to state retry policy as (count, base_ms, cap_ms, jitter) or 'no retry'.
- Failure Modes: make the EARS citation verifiable. Add: 'Each Recovery cell MUST cite a PN-ACn that EXISTS in delivery-plan.md; a citation with no matching criterion is drift and is REJECTED. The id must be a real anchor, not a placeholder PN-AC?.' (Currently every green example uses PN-AC? which is itself stub-passable.)
- Startup Sequence: require a readiness rule. Add a mandatory step/criterion: '/health SHALL return non-200 (503) until create_tables() completes' as an EARS-Unwanted criterion with a test, so readiness-before-bootstrap is not invented. Also require each numbered step that is externally observable to name its observable signal (log line or probe), not just describe a transition.
- Add a required 'Concurrency & Retry Policy' subsection (or rows): single-writer vs pooled, lock-acquire timeout in seconds, and the retry policy template (count/base/cap/jitter) referenced by both Inter-Component Contracts and Failure Modes — eliminating the per-cell reinvention.
- Add a required 'Observability' row/subsection that links the existing harness/patterns/observability.md and forces: request-id format, request-id propagation across the topology edges, and the per-failure log fields. This anchors the 'surface request id' phrasing that currently floats in the green examples with no contract.
- Phase Notes > 'Contract frozen for later swap': require an anchor, not a phrase: 'cite the exact section + anchor in api.md/data-model.md/agent-graph.md that holds the frozen shape (e.g. api.md §POST /query → Response 200). A bare phrase like "/query response shape" is REJECTED.'

## `/Users/sai/Workspace/Code/exp1/spec/data-model.md` — score 4/5 (revise)
**Stub-passable sections:**
- Entities > Notes column: the only free-text field per row, and it carries the highest-value semantics (units, JSON shape, value ranges, encoding) with ZERO format rules. A researcher can leave every Notes cell as a vague phrase or the literal '<!-- -->' shown in the template rows (id/created_at have empty Notes in the seed) and the table still 'looks complete'. Nothing forces JSON column shape to be specified beyond the optional 'e.g. array<object{...}>' hint on column_schema.
- Entities > JSON-typed fields: rule 2 accepts 'JSON' as a concrete type, but a JSON blob is exactly where structure hides. The template lets `column_schema JSON` pass with no required sub-schema. An executor cannot build a parser/validator from 'JSON' alone — the inner contract is hand-waveable.
- Storage Engine > 'Why this engine' is prose-gated by a warning ('Vague answers like "it's lightweight" are rejected') but there is no machine-checkable rule. A researcher can write one plausible sentence naming an access pattern that may not match reality; nothing ties the claim to the actual query workload in api.md/agent-graph.md.
- Entities > field NULL semantics: only Required yes/no is forced. The meaning of NULL (e.g. dataset_id null = 'no dataset bound' vs 'pending') is unconstrained. rows_affected 'nullable: null for llm rows' is given as an example but not required for the analogous fields a researcher adds.
- audit_log > token/model columns: the OTel columns (model, input_tokens, output_tokens) are a single optional FILL-IN row with 'e.g.' framing. A researcher can drop them entirely and still satisfy the table, leaving the latency/cost observability story unmodelled despite the harness's stated observability bar.
- Relationships > Rationale column: free text, no rule. Easily filled with a tautology ('because they relate'). Only On-delete is hard-gated.
- Sensitive Fields > Classification column for uploaded-data: 'low/PII?' is offered as a guess. A researcher can mark uploaded user data 'low' with no justification and pass; the template never forces a data-classification decision procedure for ingested columns whose content is unknown at spec time.
- Lifecycle > 'Updated by' for entities the researcher adds: the seed rows model never/append-only, but a newly-added mutable entity has no forced statement of WHICH fields are mutable vs immutable — only a single 'Updated by' endpoint name, not a per-field mutability contract.
**Missing for executor:**
- Index / uniqueness strategy beyond PK: the template forces PK and allows UNIQUE/CHECK but never REQUIRES declaring secondary indexes or composite uniqueness. An executor must invent indexes for hot lookups (e.g. audit_log by session_id+created_at, query by session_id) — directly impacting whether the latency criterion is even achievable. No 'Indexes' row/section exists.
- JSON column inner schema: 'column_schema JSON' and 'payload TEXT' give no field-level shape, so an executor inventing the (de)serialization contract risks mismatching api.md response bodies. Needs a required nested-schema block for every JSON/structured-TEXT column.
- Numeric units & precision: REAL/DOUBLE is allowed but the template never forces units (ms? bytes? rows?) or precision/scale for money-like or measured values. duration_ms is named by convention only; a researcher could add `cost` REAL with no currency/scale and the executor must guess.
- String length / max-size bounds: TEXT is unbounded and the template never asks for max lengths or upload-size caps (e.g. dataset name, payload SQL). Executor must pick limits, which affects validation in api.md and DoS surface.
- Default values for non-timestamp columns: rule 5 forces DEFAULT only on TIMESTAMP. status, row_count, booleans, enum columns have no forced DEFAULT/NOT-NULL-without-default decision, so insert-time required-field behaviour is left to the executor.
- Collation / case-sensitivity for UNIQUE text (e.g. dataset.name): not forced; executor decides whether 'Foo.csv' and 'foo.csv' collide.
- Transaction/atomicity boundary for the append-only audit write relative to the SQL/LLM action it records: the audit criterion asserts a row exists but not that the write is in the same transaction / what happens on partial failure. Executor must invent failure-atomicity semantics.
- Timezone of TIMESTAMP (UTC vs local) and whether 'now()' is wall-clock or monotonic — unspecified; affects every created_at and the latency ledger.
**Fixes:**
- Entities: split the single 'Notes' column into typed, gated sub-requirements, or add a mandatory rule: 'For every JSON or structured-TEXT field, attach a fenced sub-schema block (field:type per key, with required/optional and value bounds). A bare type JSON/TEXT for a structured field is rejected.' Make the column_schema example MANDATORY shape, not 'e.g.'
- Storage Engine: add an 'Indexes' subsection that is REQUIRED — one row per non-PK lookup path the API/agent performs (table | columns | why | which endpoint/node drives it), and gate it: 'Any column named in a WHERE/JOIN in api.md or agent-graph.md must have a declared index or an explicit [ASSUMPTION: full-scan acceptable, <N rows]'. This directly de-risks the latency criterion.
- Entities: add rule 7 — 'Every column states a DEFAULT or is explicitly NO-DEFAULT (must be supplied on insert). NOT NULL without default must name the writer that always supplies it.' Extend the forced-DEFAULT rule beyond TIMESTAMP to enums/booleans/counters.
- Entities: add rule 8 — 'Every numeric column states units in the field name suffix or Notes (e.g. _ms, _bytes, _rows) and, for REAL/DOUBLE, precision/scale or [ASSUMPTION]. A unitless number is rejected.' And rule 9 — 'Every TEXT column that is user-supplied states a max length or [ASSUMPTION: unbounded, justified].'
- audit_log: promote the OTel token/cost columns from one optional 'e.g.' row to REQUIRED rows whenever architecture.md Stack lists an LLM (model TEXT, input_tokens INTEGER, output_tokens INTEGER, gen_ai.* mapping). Tie it to a CHECK on token counts >= 0. This matches the harness observability bar instead of leaving it droppable.
- Audit criterion: strengthen atomicity — add a second EARS line: 'WHILE recording an action, the system SHALL write the audit_log row in the same transaction as the action so that a failed action leaves no audit row (or an action with status=error leaves exactly one row).' Force the acceptance test to assert the failure path, not just the happy path.
- Add a top-level 'Timestamps & timezone' note gated as REQUIRED: 'All TIMESTAMP columns are UTC; now() = CURRENT_TIMESTAMP in UTC. State any exception.' Removes the per-column guessing.
- Sensitive Fields: replace the 'low/PII?' guess with a forced decision rule — 'For any column holding user-uploaded content of unknown shape, classification MUST be the most-restrictive applicable (PII-unknown -> treat as PII) unless the researcher documents why the ingest path cannot contain PII.' Add a required protection statement for uploaded-data-at-rest (encryption / local-only / deletion).
- Sensitive Fields & Lifecycle: add a per-field MUTABILITY column to Entities (immutable / mutable-by:<endpoint>) so that 'Updated by' in Lifecycle is derived from a per-field contract rather than a single table-level endpoint name.

## `/Users/sai/Workspace/Code/exp1/spec/api.md` — score 4/5 (revise)
**Stub-passable sections:**
- Acceptance-test cells throughout (§Surface line 65, §Endpoints copy-template lines 162-165, §Health line 124, §Auth line 193, §Stub line 215): the test column is always pre-filled as an `<!-- e.g. ... -->` comment and the template never declares 'a test cell left as a comment / pointing at "see tests/" is REJECTED'. A researcher can ship strong EARS sentences with no executable test node and pass.
- Traces line (endpoint copy-template line 135, index table Traces column, §Health line 100): '≥1 required' is prose-only and unverifiable from inside api.md — any id string (real, wrong, or invented) satisfies it; nothing forces the cited SC-N/PN-ACn to resolve to a real row in vision.md / delivery-plan.md.
- Error matrix (copy-template lines 154-160): no minimum-coverage rule. A mutating endpoint can list a single 500 row and omit 422 (bad input) and 404 (not found); the three example rows are illustrative, not mandated.
- Request fields (copy-template lines 137-142): `name: type — meaning` is enforced but field-level CONTRACT is not — required-vs-optional, nullability, enum/allowed values, min/max length, and id format-per-field can all be omitted. `dataset_id: string — the dataset` passes every stated rule yet defines no validation surface.
- Primary success response quantity (copy-template Response 200 lines 146-151 + EARS line 164): the strong bar ('rows SHALL contain ≥1 object with the SELECTed columns') lives only in the Weak/Strong EXAMPLE; the FILL-IN prose mandates 'min count' only for list fields, so a correctly-typed 200 block paired with a weak 'returns 200 with rows' EARS clears every explicit rule.
- Status-code semantics: 201 + Location for creates is a soft `<!-- If the endpoint creates a resource -->` aside (line 152); 200 vs 202 (async) is unaddressed. An executor can invent the success status with no template pushback.
**Missing for executor:**
- The exact executable acceptance test (pytest node id or curl + asserted field/value) for every endpoint — currently always a comment, so the executor must author the binding test from scratch.
- Per-field request validation rules (required/optional, nullable, enum/allowed set, length/format bounds) — needed to implement the 422 path and Pydantic models without guessing.
- The full error-code set per endpoint (which 4xx conditions exist and their exact trigger) — a stub matrix omits the validation/not-found rows the executor must still handle.
- Success status-code policy for creates and async work (201+Location? 202+poll?) — the executor will otherwise invent it.
- Concrete pagination contract for any list endpoint — the template offers the envelope as an optional `<!-- FILL IN -->` (lines 56-61); if a researcher writes 'none' wrongly, the executor inherits an unbounded list with no param types.
- Whether each documented-but-later-phase endpoint is stub-served in Phase 1 and the exact canned shape — cross-ref to architecture Phase Notes exists but the stub VALUE/shape per endpoint is not pinned in api.md.
**Fixes:**
- In §Endpoints FILL-IN (after line 83) add a hard rule: 'The acceptance-test cell MUST contain a runnable reference (pytest node path OR full curl) plus the asserted field and value. A test cell left as an `<!-- e.g. -->` comment, blank, or a bare "see tests/" is REJECTED.' Mirror the same sentence under §Surface, §Health, §Auth, §Stub so no EARS table can ship without an executable test.
- Add a Traces-resolution rule: 'Every id in Traces MUST be an existing row id in vision.md (SC-N) or delivery-plan.md (PN-ACn). The analyser rejects a trace that does not resolve.' Optionally require the cited EARS text be quoted inline so a wrong id is visible.
- Add an error-matrix coverage rule to the copy-template: 'Any endpoint with a request body or path/query params MUST enumerate at least its 422 (validation failure) and, if it resolves a resource by id, its 404 row, each with a precise trigger condition. A matrix with only 5xx rows is REJECTED for an input-taking endpoint.'
- Upgrade the request block spec (lines 78-83 and 137-142) to require per-field contract annotations: change the field grammar to `name: type [required|optional] [nullable] [constraint] — meaning`, where constraint names the enum/allowed set or min/max/format. Add a forbidden example: `dataset_id: string — the dataset` (no required flag, no format) is REJECTED.
- Promote the strong success bar from example to rule: in the copy-template require 'the FIRST EARS row of every non-/health endpoint SHALL assert a named success field carrying a quantity (≥1 / non-empty), not merely status 200.'
- Add a success-status rule near line 152: 'A create endpoint MUST return 201 with a Location header; an async/long-running endpoint MUST document 202 + the poll/stream contract. Do not default to 200 for creates.'
- Tighten the pagination instruction (lines 56-61) from optional to a forced choice: require the researcher to write EITHER the typed envelope OR the exact sentence 'No pagination — list endpoints return the full set; bounded by [reason]' — and add a Weak/Strong pair so 'pagination TBD' is explicitly rejected.
- Add a per-endpoint Phase-1 stub line to the copy-template: 'Stub shape (Phase 1): the exact canned response served when APP_LLM_PROVIDER=stub — same fields, real quantities (e.g. rows length ≥1)', so the contract-first/stub-swap promise is pinned in api.md, not only referenced in architecture Phase Notes.

## `/Users/sai/Workspace/Code/exp1/spec/ui.md` — score 4/5 (revise)
**Stub-passable sections:**
- Screens > Four-state tables: each state cell is free prose. The 'name a quantity/field' rule lives in an HTML comment, not in a structural constraint. A researcher can write 'Populated = table renders' / 'Empty = no results message' and the template is satisfied — the actual teeth are in delivery-plan.md PN-ACn, not enforced here. A screen can be filled with one happy-path sentence per state and pass.
- Screens > Key elements & Actions: tables permit a single example row. Nothing forces enumeration of every element/action on the screen, so a researcher can list one element and leave the rest implicit; the executor invents the rest.
- Component UX Bar > 'Phase AC' and 'Serves SC' columns: the template ships most rows with these cells blank and only a comment asks for bidirectional trace. There is no rule that EVERY row must be filled, so a researcher can leave traceability empty and the file still looks complete.
- Component UX Bar criteria themselves: the 14 EARS rows are PRE-WRITTEN in the template for one specific product (query/table/chart/chips/sessions). A researcher building a different UI can keep them verbatim (they 'satisfy the template') even though they describe the wrong product — or gut them and the template provides no replacement floor. The rigor is borrowed from one example domain, not forced for the actual domain.
- Global shell ASCII block: 'concrete ASCII, NOT TODO' is satisfied by any box-drawing; there is no assertable property (must show banner row, must show the named regions), so a hand-drawn box that omits regions still passes.
- Open questions table: 'never leave a blank cell' is a comment-level instruction with no enforced minimum; the section can be deleted as 'genuinely empty' with no check that defaults were actually surfaced as [ASSUMPTION] rows.
**Missing for executor:**
- Exact loading-skeleton geometry: 'skeleton matching content shape' is qualitative. The executor must invent how many skeleton rows, what widths, whether the chart area also skeletons. No numeric contract (e.g. 'render exactly page-size skeleton rows').
- Pagination page size and overflow behavior: criterion 3 says 'paginate or virtualise' and the page size only appears as an example [ASSUMPTION: 50] in Open questions. The executor must decide page size, whether it is server- or client-side, and where the control renders — none is forced.
- Error-toast lifecycle: 'transient' is unquantified — no dismiss timeout, no max stacked toasts, no behavior when multiple errors fire. Executor invents it.
- Empty-vs-Loading disambiguation on first paint: nothing specifies what shows before the first query (Empty CTA) vs while stub /health is being fetched (Loading), so the executor guesses the initial render.
- Responsive/breakpoint behavior of the shell (sidebar collapse on narrow viewports): the ux.md pattern mentions grid responsiveness for charts but ui.md sets no breakpoint contract for the global shell, so mobile/narrow layout is undefined.
- Which screens actually exist: the Screens section is a single example subsection plus a 'repeat for every screen' comment. For a given product the inventory is entirely researcher-supplied with no minimum beyond 'the shell screen', so a thin fill yields one screen and the executor invents the rest of the navigation graph.
**Fixes:**
- Add an explicit FILL RULE to Component UX Bar: 'Every row's Phase AC and Serves SC cells MUST be non-empty; a row with either blank is REJECTED at the pre-code gate.' Make the trace mandatory in prose, not just in a trailing comment.
- Replace the pre-written 14 example criteria with a STRUCTURE the researcher must instantiate per-product: keep the columns and the Weak/Strong box, but move the 14 query-app criteria into a clearly-marked '<example for a data-query UI — DELETE and replace with criteria for THIS product>' block, and add a hard floor: 'At least one criterion per data-bearing screen, each naming a quantity or named field, each with a runnable acceptance test. A criterion satisfiable by empty-list+200 is REJECTED.' This stops the borrow-the-example-product failure.
- Put teeth on the four-state table: add a per-cell constraint line under the Screens FILL block — 'Each state cell MUST name a concrete artefact: Loading = exact skeleton count/shape; Empty = the literal CTA string; Populated = a named field AND a quantity (row count / axis label); Error = the literal user-facing message string. Prose like "table renders" or "shows results" is REJECTED.' Mirror the Weak/Strong box used elsewhere with a four-state example.
- Force element/action enumeration: change the Key elements and Actions guidance from 'a table or bullet list' to 'enumerate EVERY interactive element and EVERY user action; a screen listing only an example row is REJECTED. Each Action row MUST cite an api.md endpoint id or an explicit follow-up.'
- Quantify the shell ASCII: require the ASCII to label the stub banner row, the named regions from the Global shell table, and the active-screen marker — 'the diagram MUST show every region marked "Always present? yes"; a box omitting a mandatory region is REJECTED.'
- Add a small 'Cross-screen interaction contracts' subsection capturing what is currently executor-invented: pagination page size (or cite the api.md/data-model owner), error-toast lifecycle (timeout, max stack), and initial-paint state (Empty CTA vs Loading) — each as one EARS row with a test, or an explicit [ASSUMPTION].
- Add a screen-inventory floor: 'List every screen reachable in the product; the inventory MUST cover every UI-bearing phase in delivery-plan.md. A single-screen fill for a multi-screen product is REJECTED.' Cross-check against delivery-plan phases at the gate.

## `/Users/sai/Workspace/Code/exp1/spec/agent-graph.md` — score 4/5 (revise)
**Stub-passable sections:**
- Nodes > Behaviour: the per-node behaviour paragraph is enforced only by a prose weak/strong EXAMPLE, not by structure. A researcher can write 'Prompts the model with the question and validates the output, writing it to state.x' — which is vague but ticks the one-paragraph box. There is no required structured sub-field for (a) the exact prompt inputs, (b) the guard PREDICATE as a checkable expression, or (c) the named output field. The single most load-bearing node contract is the softest cell in the file.
- Nodes > Serves: 'internal' is an unconditioned escape hatch. The rule only obliges SC-N/PN-ACn citation for nodes 'that produce user-visible output', and the researcher self-classifies. Every node can be marked 'internal' to dodge all traceability, and the template never cross-checks that some node serves each SC.
- Nodes > External calls > Operation cell: no format constraint. 'NL→SQL completion' is the example, but 'calls the API' or 'LLM call' fits the cell equally and passes. Only the On-failure cell is constrained to an enum.
- Edge Topology > diagram: the diagram lives INSIDE an HTML comment as an 'Example shape'. A researcher can delete the comment fence and ship the generic example (node_first/node_second/node_route) verbatim. Unlike State fields, diagram node names are NOT cross-checked against the Nodes section by name, so a diagram that doesn't match the real nodes still passes.
- Concurrency & Checkpointing > 'partial: log + continue' degrade path: a node may declare a partial failure and 'still write a usable value' with no constraint on WHAT the usable fallback value is, so degraded behaviour is unspecified-but-passable.
- [NEEDS CLARIFICATION]/[ASSUMPTION] markers are defined but never REQUIRED by any section. A researcher can leave zero markers and the template is silent; topology-changing unknowns (HITL interrupt? retry loop?) can go un-surfaced.
**Missing for executor:**
- The exact guard/validation predicate per node. Behaviour says 'plus the validation/guard it enforces' in prose, but the executor must invent the concrete check (e.g. reject non-SELECT SQL, max length, schema-conformance) because no cell forces it as a testable expression.
- The prompt content / message assembly for any LLM node — which state fields go into the prompt, system vs user role, and the expected output parse. The template defers all of this to src/ but gives the executor no contract to code against, so two executors produce different prompts.
- Retry / backoff policy on 'fatal' external calls. On-failure is a binary fatal|partial; there is no place to specify retry count, timeout, or idempotency, so the executor invents resilience behaviour.
- The concrete response object SHAPE. node_finalize 'assembles the final response' and says cross-ref api.md, but the template carries no field-level response contract here; if api.md is also loose the executor invents the shape.
- Timeout/latency budget per node. Concurrency revisit trigger mentions 'run latency > N s' as an example only; no per-node or per-run budget is forced, so the executor has no SLA to honor.
- Stub-mode behaviour of each node. AG-AC3 asserts the RUN completes in stub_mode, but per-node stub fallbacks (what value a stubbed LLM node writes) are not specified, leaving the executor to invent fake outputs.
**Fixes:**
- Nodes: replace the free-text 'Behaviour' paragraph with a 4-row required sub-table that is hard to hand-wave: | Prompt inputs (state fields) | Output field written | Guard (predicate that must hold, as a checkable expression) | On guard fail |. Reject any node whose Guard cell is not a boolean expression over state/output (mirror the State section's 'no bare Any' rigor). Keep one prose sentence only for the transformation summary.
- Nodes > Serves: remove 'internal' as a free pass. Require EITHER a cited SC-N/PN-ACn OR an explicit 'internal: serves <named node> by producing <field>' justification. Add a closing rule under Nodes: 'Every SC-N referenced in vision.md MUST be served by at least one node; an SC with no serving node is a CRITICAL gap.' This makes Serves enumerable like the State cross-check.
- Edge Topology: move the diagram OUT of the HTML comment into a live fenced ``` block that the researcher MUST overwrite, and add a hard bar: 'Every node name in this diagram MUST appear verbatim in the Nodes section, and every Nodes-section node MUST appear here — a name mismatch is rejected (same enumerable cross-check as State fields).' Add an explicit instruction: 'Do NOT ship the example node names (node_first/node_second/node_route).'
- External calls > Operation: constrain the cell like On-failure is constrained — require the form '<verb> <object> (<model id / table / endpoint>)', e.g. 'NL→SQL completion (gemini-2.x, see architecture.md)'. A bare 'API call' is rejected.
- Add a per-node 'Resilience' column or row to the External-calls table: | Timeout | Retries | Idempotent? | so the executor codes resilience to contract, not invention. Default it explicitly (e.g. [ASSUMPTION: timeout=30s, retries=0]) rather than leaving silent.
- Add a required 'Stub fallback' line to each node's external-calls block: for stub_mode/no-key, state the exact deterministic value the node writes. Pair it with AG-AC3 so the run-completes test also asserts the stubbed field value, not just 'a populated response'.
- Make markers mandatory: add a top-of-file checklist line 'List topology-changing unknowns here as [NEEDS CLARIFICATION]; if none, write "No open topology questions." — a blank is rejected.' This forces the researcher to affirmatively clear the HITL/retry-loop questions.
- Fix the stale cross-reference: lines 28 and 258 point to harness/patterns/langgraph.md, but the file is a 'Usage-spec' and per the repo's direction usage-specs move to spec/patterns/ and patterns/ was just removed from spec. Repoint to the canonical location and add a one-line note so the link can't dangle.
- node_finalize: add a required minimal response-shape row here (field: type) flagged 'must equal api.md response body' rather than only a cross-ref, OR add an explicit assertion in AG-AC1 that the response contains each named output field — so a loose api.md cannot leave the response shape uninvented.

## `/Users/sai/Workspace/Code/exp1/spec/delivery-plan.md` — score 4/5 (pass)
**Stub-passable sections:**
- Phasing Model (§): pure restatement of normative rules — a researcher copies the boilerplate table verbatim; it contains zero project-specific commitments, so it always 'passes' without saying anything about THIS product. Harmless but it dilutes the doc's claim that 'the researcher fills every section thoroughly'.
- Phase Overview > 'Theme (one line)' column: forced to be 'capability not layer', but nothing structurally prevents a vague capability theme ('better answers', 'improved UX'). The anti-layer rule is enforceable by eyeball but not by a mechanical check; a lazy one-liner survives.
- Phase Overview > 'Est. build' for phases 2..N: only Phase 1 has a hard ceiling (≤30 min). Later-phase estimates ('~40 min' in the example) are unconstrained and unvalidated — a researcher can write any number and nothing gates it. Estimates carry no contract.
- Later Phases > Phase 3+ block: the template gives Phase 2 a full worked example but degrades to 'Same structure as Phase 2' for Phase 3 (lines 234-238). A researcher can under-fill Phase 3+ (skip the EARS table, skip Depends-on) and the looser prose instruction gives cover. The rigor is front-loaded onto Phase 1/2 and trails off.
- Gaps & Assumptions: a researcher who wants to look thorough can dump genuine unknowns into [ASSUMPTION: ...] rows here and thereby leave PN-ACn cells filled with hand-wave upstream while claiming 'recorded in Gaps'. The section is a legitimate escape valve but also a place to launder vagueness.
- (e)/(§ Explicitly Deferred) consistency-with-vision.md columns: enforced only by the author citing a vision.md Non-Scope row by name. There is no mechanical bidirectional check inside THIS file — a researcher can cite a non-existent or mismatched Non-Scope row and the cell still reads as filled.
**Missing for executor:**
- Per-phase RUN/START commands are only demanded inside Phase 1's Golden Path (§b, 'exact start command'). Later phases' acceptance tests cite 'eval golden case' / 'pytest node' but the template never forces the EXACT command, file path, or eval-case id to exist — an executor reading P2-AC1 ('eval golden case top 5 products PASS at threshold') must still invent the eval case file, its location, and the threshold value (those live in evals/, which this file never points to concretely).
- The stub→real SWAP mechanics are named ('reference the stub contract it replaces') but the template only asks for an api.md/ui.md *id*. It does not force the researcher to state WHAT field-level change makes the value real while the shape stays frozen (e.g. 'rows now come from DuckDB execution, response schema byte-identical to P1 stub'). An executor still has to infer which bytes may change vs must not.
- Concurrency / ordering within a phase: the doc is vertically sliced per phase but says nothing about whether the parallel steps INSIDE a phase (the non-negotiable's 'parallel steps') have their own contract freeze points. An executor parallelizing FE/BE inside Phase 2 has no per-phase freeze list — only the cross-phase one in §Inter-Phase Dependency Map.
- Test-data fixtures: PN-ACn tests reference 'sample.csv', 'a known-answer query', 'top 5 products' but the template never forces the researcher to pin the exact fixture file, its row count, or the expected answer values. 'WHEN a known-answer query … ≥1 row' is satisfiable by ANY non-empty result; the executor must invent what the correct answer actually IS.
- Error-code registry: P1-AC4/P2-AC3 require named error codes (BAD_SQL, 'Unsupported file') but the template does not force a per-phase error-code list nor point to where api.md's error matrix is reconciled — the executor must cross-walk codes manually between this file and api.md.
**Fixes:**
- Phase Overview: add a REQUIRED 'Est. build' constraint for ALL phases, not just Phase 1 — e.g. 'each phase ≤ build ceiling; state the number AND the one capability that would breach it (forcing a deferral row)'. Without an upper bound, later estimates are decoration.
- Later Phases Phase 3+: delete the 'Same structure as Phase 2 (prose)' shortcut at lines 234-238 and instead paste the FULL empty Phase-2 block (Scope / EARS table with PN-ACn rows / Depends-on / hard gates / Still-deferred) for Phase 3 as a literal to-fill skeleton. Make under-filling structurally visible (empty cells), not prose-excusable.
- Acceptance Criteria tables (all phases): add a mandatory column 'Fixture + expected value' that forces the exact test data and the exact asserted number/string (e.g. 'sample.csv, 100 rows; query top 5 → returns rows [Widget A … Widget E], row_count==5'). Today 'row count ≥ 1' is the floor; raise it to an exact expected value so a wrong-but-non-empty answer fails.
- Acceptance Criteria 'Acceptance test' cells for later phases: require the eval-case to be cited by FILE PATH + case id (e.g. evals/cases/top5.json#case_id) and the threshold by value, not just 'PASS at threshold'. Add a note that an eval gate line with no concrete case path is INCOMPLETE → gate fails.
- Stub→real swap: in the Later-Phases Scope table, change 'names the stub contract replaced' to require TWO things — (1) the api.md/ui.md id, AND (2) an explicit 'shape-frozen / value-changes' line stating which fields change vs which bytes are guaranteed identical. This pins the in-place-swap contract the executor needs.
- Add a mechanical traceability ledger at the bottom: a small 'SC → Phases' and 'PN-ACn → SC' two-column matrix that must be filled and self-checked, rather than the prose 'COVERAGE CHECK' comment. A table is auditable at the gate; a prose assertion is not.
- Phasing Model section: replace the verbatim-restated normative table with a single line 'Phasing rules are normative in [top-level phasing_model] — not restated here' OR require it to add ONE project-specific sentence per rule (how rule N binds THIS product). As-is it is pure boilerplate that inflates the appearance of thoroughness.
- Error codes: add a per-phase 'Error codes introduced' mini-row that must match api.md's error matrix by id, so the executor has a single reconciled list instead of cross-walking two files by hand.

