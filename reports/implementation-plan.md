# Implementation Plan — Standup Bot

## Phase 1 — Domain Models + DB Schema
Gate: unit tests for DB insert/query pass with test DB

## Phase 2 — Core API Loop (Stubbed)  
Gate: POST /slack/standup saves to DB; GET /standups returns records; integration test passes offline
